# NBA 关键时刻球员表现分析 — 增量 PRD (NBACore Studio v8)

- **文档版本**：v1.0（增量 PRD，仅需求与设计口径，不含实现）
- **作者**：许清楚 (Xu) — Product Manager
- **关联模块**：指标引擎 / 数据层 (`play_by_play`，库 `nba` 端口 5433)
- **文档类型**：增量需求定义

---

## 1. 产品目标

为分析师与内容创作者提供"比赛最后 5 分钟、分差 ≤5 分"关键时刻下球员表现的量化视图，覆盖命中率、出手选择、接球/运球终结方式与基础事件数据，支撑 Top 30 球员排名与可信的内容产出。

## 2. 用户故事

- 作为篮球分析师，我希望按关键时刻筛选比赛回合并按球员聚合统计，以便快速定位"关键时刻先生"。
- 作为内容创作者，我希望看到球员出手选择（跳投 / 上篮 / 扣篮等）与接球 / 运球终结的拆分，以便撰写有细节支撑的故事线。
- 作为数据用户，我希望无法区分的字段（如 `nba_api` / `BBRef` 源的运球信号）被明确标注为 N/A，以便我信任口径而非误读。

## 3. 关键定义与口径

### 3.1 关键时刻窗口规则 (Clutch Window)

- **基础规则**：取 `clock_seconds <= 300` 且 `abs(scoremargin_numeric) <= 5` 的回合。
- **`scoremargin` 解析**：源字段为字符串（如 `'+5'`、`'-3'`、`'TIE'`）。需解析为数值：`'+5'`→5、`'-3'`→-3、`'TIE'`→0，再做 `abs() <= 5`。**字段精确格式（正负号、是否含空格/大小写）需在设计中由架构师确认**（见 §5 待确认问题 ⑤）。
- **`clock_seconds` 解析**：应为整数秒（0–3000），取 `<= 300`。**精确格式（是否整数、空值 / 0 语义）待架构师确认**（§5 ⑤）。
- **节次范围（默认建议）**：仅统计**常规时间第 4 节**（`period = 4`），满足"比赛最后 5 分钟"语义；加时赛（`period >= 5`）**默认排除**，除非确认纳入。
  - *待确认*：是否包含加时？加时的"最后 5 分钟"需用对应 period 的 clock 重新界定，逻辑不同。
- **跨源一致性**：同一 `gameid` 可能出现在多源（`nba_api` / `br_crawler` / `BBRef`）。聚合**按源分别统计后再合并**还是**按 `gameid` 去重**需明确。建议：同一回合可能被多源记录，按"主源优先"合并避免重复计数；默认主源 = `br_crawler`，回退 `nba_api` / `BBRef`。

### 3.2 出手选择分类 (Shot Selection)

按 `subtype` 族分类（仅对 `br_crawler` / `BBRef` 有语义；`nba_api` 的 `subtype` 为数字编码，需走特殊分支）：

| 类别 | 匹配规则（subtype 含） | 备注 |
|---|---|---|
| Jump Shot | `Jump Shot`、`Pullup Jump shot`、`Step Back Jump shot`、`Running Jump Shot`、`Turnaround Jump Shot` | 含运球子类，见 §3.3 |
| Layup | `Layup Shot`、`Driving Layup Shot`、`Running Layup Shot` | |
| Dunk | `Dunk Shot` | |
| Hook | `Hook Shot` | |
| Tip | `Tip Shot` | |
| Free Throw | `event_type='free throw'` 或 subtype 含 `Free Throw` | 不计入 FG% 分母 |
| Other / Unknown | 无法归类 | 标记，不计入主要口径 |

**2 分 vs 3 分拆分**：

- `br_crawler` 的 `subtype` **未直接标 2/3 分**（如 `Jump Shot` 既可能是 2 分也可能是 3 分）。
- **推导方案（建议）**：优先用得分增量（`h_pts` / `a_pts` 的该回合增量，命中 +2 或 +3）；或解析 `description` 前缀（`3-pt` / `3pt` 标记）。**待确认问题 ①** 正式选定推导规则。
- `BBRef` 的 subtype 直接含 `2-pt` / `3-pt`，可直接分类。
- `nba_api` 数字编码无法分类 → 标记 N/A。

### 3.3 接球终结 vs 运球终结 (Catch-finish vs Dribble-finish) — 覆盖率警告

**仅 `br_crawler` 源可区分**（其 subtype 含运球信号）：

- **运球终结 (Dribble-finish)**：subtype 含 `Pullup` / `Step Back` / `Running` / `Driving` / `Turnaround` → 视为"运球后出手"。
- **接球终结 (Catch-finish)**：plain `Jump Shot` / `Layup Shot` / `Dunk Shot` 等无运球信号词 → 视为"接球即出手"。
- **覆盖率警告**：`nba_api`（数字编码）与 `BBRef`（无 pullup / 运球信号）**无法区分**此维度。对这些源 / 回合，该字段**必须记为 `N/A` 或"未知"**，且 Top 30 排名与图表需展示"运球可识别覆盖率"指标，避免误导。
- 建议后端返回每球员的 `catch_dribble_coverage_rate = 可识别运球信号的出手数 / 该球员总出手数`，前端显著标注。

### 3.4 命中率口径 (FG%)

- **FG%** = 命中投篮数 / 出手投篮数（不含罚球）。
- **2P% / 3P%**：按 §3.2 的 2/3 分拆分分别统计。
- **FT%**：按罚球出手 / 命中统计。**是否纳入"命中率"总口径待确认（§5 ④）**。默认 FG% 不含 FT%，FT% 单独列。

### 3.5 基础事件统计（关键时刻内）

- **篮板**：建议拆分**进攻篮板 / 防守篮板**（按 `event_type` 或 subtype 区分；待架构师确认字段映射）。
- **助攻、抢断、失误、犯规**：按 `event_type` 聚合计数。
- **出场回合 (possessions)**：该球员在 clutch 窗口内出现的回合数（用于排名默认口径）。

### 3.6 排名口径 (Top 30)

- **默认**：按关键时刻**出场回合数降序**（保证样本量，过滤极端小样本）。可选加最小回合阈值（如 ≥ 10 回合）以避免噪声。
- **备选 A**：按关键时刻**得分 (points)** 降序。
- **备选 B**：按关键时刻**净效率 (net rating = (得分 − 失分) / 百回合)** 降序（需对手数据，复杂度高，降为 P1/P2）。
- 前端提供切换控件，默认 = 出场回合。

## 4. 需求池

### P0 (Must have)

- **P0-1** 实现 clutch 窗口过滤（`clock_seconds <= 300` 且 `abs(scoremargin) <= 5`，默认 `period = 4`）。
- **P0-2** 球员聚合：FG% / 2P% / 3P% / 出手选择分类 / 篮板（进攻 + 防守）/ 助攻 / 抢断 / 失误 / 犯规。
- **P0-3** 接球 vs 运球终结分类（仅 `br_crawler`），对不可区分源标记 N/A + 返回覆盖率。
- **P0-4** Top 30 排名（默认出场回合降序，可切换口径）。
- **P0-5** 跨源去重 / 主源合并策略，避免重复计数。
- **P0-6** 前端排名表渲染（见 §6 UI 草图）。

### P1 (Should have)

- **P1-1** FT% 独立统计与展示。
- **P1-2** 最小样本阈值过滤（如 < 10 回合球员降级或标注）。
- **P1-3** 覆盖率指标可视化（运球可识别率）。
- **P1-4** 赛季筛选器（全部 / 仅 2026）。

### P2 (Nice to have)

- **P2-1** 净效率排名口径（备选 B）。
- **P2-2** 加时赛纳入开关。
- **P2-3** 球员详情下钻（点击展开该球员 clutch 出手分布）。
- **P2-4** 导出 CSV。

## 5. 待确认问题 (Open Questions)

1. **2 分 vs 3 分推导**：`br_crawler` 的 subtype 未直接标 2/3 分。建议用得分增量（`h_pts` / `a_pts` 回合增量）或 `description` 前缀（`3-pt`）推导；请确认主规则与回退规则。
2. **统计范围**：覆盖全部赛季还是仅 2026 赛季？（`BBRef` 为 2026 新补，`nba_api` / `br_crawler` 跨多赛季。）
3. **排名默认口径**：确认默认 = 出场回合数降序（而非得分）。
4. **"命中率"是否含 FT%**：FG% 默认不含 FT%，FT% 单独列；请确认。
5. **字段精确格式**：`scoremargin`（字符串如 `'+5'` / `'TIE'` 的正负号、空格、大小写）与 `clock_seconds`（是否整数、空值 / 0 语义）的精确格式需架构师在指标引擎设计中最终确认。

## 6. UI 草图 — Top 30 排名表

```
┌────┬──────┬────┬────────┬─────┬─────┬─────┬──────────┬──────────┬──────┬────┬────┬────┬────┐
│排名│ 球员 │球队│出场回合│ FG% │2P%  │3P%  │ 接球出手 │ 运球出手 │ 篮板 │助攻│抢断│失误│犯规│
├────┼──────┼────┼────────┼─────┼─────┼─────┼──────────┼──────────┼──────┼────┼────┼────┼────┤
│ 1  │ 球员A│ HOU│  142   │58.2%│61.0%│40.0%│   18     │   9(N/A) │  22  │ 11 │ 5  │ 3  │ 7  │
│ 2  │ 球员B│ LAL│  138   │ ... │ ... │ ... │   12     │   4      │  ... │    │    │    │    │
│... │  ... │    │        │     │     │     │          │          │      │    │    │    │    │
│ 30 │ 球员Z│ MIL│   51   │ ... │ ... │ ... │  3(N/A)  │  1(N/A)  │  ... │    │    │    │    │
└────┴──────┴────┴────────┴─────┴─────┴─────┴──────────┴──────────┴──────┴────┴────┴────┴────┘

注：接球/运球出手列中 "(N/A)" = 该球员 clutch 出手主要来自 nba_api/BBRef 等不可区分源；
    表头或悬浮提示展示本表"运球可识别覆盖率"。
筛选器：赛季 [全部 | 2026]  |  排名口径 [出场回合 | 得分 | 净效率]  |  最小回合阈值 [10]
```

---

*本文档为增量 PRD，定义需求与设计口径，不含任何实现代码。实现细节（SQL、指标引擎接口、前端组件）由架构师与开发团队在确认待确认问题后落地。*
