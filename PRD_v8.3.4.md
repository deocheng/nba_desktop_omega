# NBACore Studio v8.3.4 产品需求文档（PRD）

---

## 项目信息

| 字段 | 值 |
|------|-----|
| **语言** | 中文 |
| **技术栈** | 前端：原生 JS + echarts 5.5.0，暗色极简主题；后端：4层架构（Frontend → API → Engine → Data Layer） |
| **数据库** | PostgreSQL :5433/nba |
| **版本** | v8.3.4（迭代版本，当前 v8.3.3） |
| **项目代号** | nba_desktop_omega |
| **原始需求** | 三个独立功能迭代：① 爬虫页面改版（增加数据概况 + 表名输入改下拉菜单）② 球队 overlay radar 图恢复联盟平均对比 ③ Workspace 增加常用公式预设 |

---

## 产品定义

### Product Goals

1. **提升爬虫页面的可理解性和操作效率**：通过数据概况展示和下拉选表，降低用户认知负担和输入错误率
2. **增强球队 Overlay 的分析对比能力**：将联盟平均数据引入雷达图，使球队能力评估有参考基准
3. **降低 Workspace 数据分析门槛**：通过预设常用 NBA 公式，让用户无需从零创建即可快速开始分析

### User Stories

#### 需求一：爬虫页面改版

- As a **数据分析师**, I want **查看数据概况（表名、行数、说明、更新时间）** so that **我能快速了解数据库中有哪些数据、各表存什么内容，不用去翻代码或文档**
- As a **运维用户**, I want **从下拉菜单选择要爬取的表** so that **我不需要记住表名，也避免手动输入带来的拼写错误**
- As a **管理员**, I want **知道各表的数据量和最后更新时间** so that **我能判断数据是否过期，是否需要重新爬取**

#### 需求二：球队 Radar 图恢复联盟平均对比

- As a **球探分析师**, I want **在球队 overlay 雷达图中看到联盟平均对比** so that **我能直观判断球队各项指标相对于联盟平均水平的高低**
- As a **数据研究员**, I want **雷达图显示图例区分球队和联盟平均** so that **我能快速阅读图表，不必猜测线条对应哪个系列**

#### 需求三：Workspace 增加常用公式预设

- As a **初级分析师**, I want **在新建 Workspace 时看到预设的 NBA 常用公式列表** so that **我不用自己编写公式就能直接使用 EFF、TS% 等标准指标**
- As a **高级分析师**, I want **在预设公式基础上修改保存为自定义公式** so that **我能以标准指标为起点快速构建专属分析配方**
- As a **Workspace 用户**, I want **通过一键添加预设公式到 Workspace** so that **我不需要手动查找 formula_id 再逐个添加**

---

## 技术规范

### Requirements Pool

#### 需求一：爬虫页面改版

| 优先级 | 需求编号 | 需求描述 | 备注 |
|--------|---------|---------|------|
| **P0** | CRAWLER-01 | 页面顶部新增"数据概况"板块，展示各表的表名、行数（预估）、简要说明、最后更新时间 | 卡片/表格形式展示 |
| **P0** | CRAWLER-02 | 后端新增端点 `GET /crawler/table-overview` 返回表概览信息列表（从 information_schema 或爬虫元数据表获取） | API 路由层编排，Engine 层执行查询 |
| **P0** | CRAWLER-03 | 单表爬取的 `#crawlTableInput` 改为 `<select id="crawlTableSelect">` 下拉菜单 | 加载页面时自动从后端获取选项列表 |
| **P0** | CRAWLER-04 | 后端新增端点 `GET /crawler/tables` 返回可爬取表名列表 | 供下拉菜单填充选项 |
| **P1** | CRAWLER-05 | 数据概况卡片支持点击表名快速填充到爬取表下拉菜单 | 提升操作连贯性 |
| **P1** | CRAWLER-06 | 数据概况显示各表的数据状态（正常/过期/无数据） | 通过最后更新时间判断 |
| **P2** | CRAWLER-07 | 数据概况支持按表名搜索过滤 | 当表数量较多时提供搜索 |

#### 需求二：球队 Radar 图恢复联盟平均对比

| 优先级 | 需求编号 | 需求描述 | 备注 |
|--------|---------|---------|------|
| **P0** | RADAR-01 | `renderEntityTeamRadar()` 同时请求球队雷达数据和联盟平均雷达数据（两个后端端点已实现） | `GET /teams/{abbr}/radar?season=N` + `GET /teams/league-avg/radar?season=N` |
| **P0** | RADAR-02 | 雷达图绘制双系列：球队数据 + 联盟平均数据，用不同颜色区分 | 参考 `app.js` 中 `renderTeamRadarChart`（1557行起）的双系列模式 |
| **P0** | RADAR-03 | 图例显示"球队名"和"联盟平均" | 样式与现有雷达图一致 |
| **P1** | RADAR-04 | 联盟平均系列用虚线/半透明区域样式，球队系列用实线/高亮区域 | 视觉层次分明 |
| **P1** | RADAR-05 | Tooltip 显示每个指标的具体数值，区分球队值和联盟均值 | 参考 `renderTeamRadarChart` 的 tooltip 实现 |

#### 需求三：Workspace 增加常用公式预设

| 优先级 | 需求编号 | 需求描述 | 备注 |
|--------|---------|---------|------|
| **P0** | WSPRE-01 | 后端提供公式预设列表端点 `GET /formulas/presets` 返回预设公式数组 | 或通过 seed 数据写入 formulas 表，标记 is_preset = true |
| **P0** | WSPRE-02 | 预设公式包含：EFF（效率值）、TS%（真实命中率）、PER（简化版）、PIE（球员影响力估算）、ORTG/DRTG（进攻/防守效率） | 至少 5 个常用公式 |
| **P0** | WSPRE-03 | 前端 Workspace 详情页展示"可用公式清单"，用户可一键添加预设公式到当前 Workspace | 避免手动输入 formula_id |
| **P1** | WSPRE-04 | 新建 Workspace 时默认推荐预设公式列表 | 引导用户快速搭建 |
| **P1** | WSPRE-05 | 用户可从预设公式复制并修改为自己的自定义公式 | "另存为自定义"功能 |
| **P2** | WSPRE-06 | 预设公式支持分类展示（进攻类/防守类/综合效率类） | 方便用户浏览 |

### UI Design Draft

#### 爬虫页面布局（改动后）

```
┌──────────────────────────────────────────────────────────────┐
│  [page-header] Crawler Control                    [status]   │
├──────────────────────────────────────────────────────────────┤
│  ┌─ 数据概况 (Data Overview) ──────────────────────────────┐ │  ← 新增区块
│  │ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │ │
│  │ │ team_gam… │ │ player_… │ │ game_s…  │ │ injur…   │    │ │
│  │ │ 1,245 行  │ │ 8,932 行 │ │ 4,560 行 │ │ 890 行   │    │ │
│  │ │ 球队比赛… │ │ 球员赛季…│ │ 比赛赛程 │ │ 伤病报…  │    │ │
│  │ │ 更新:3/12 │ │ 更新:3… │ │ 更新:3…  │ │ 更新:…   │    │ │
│  │ └──────────┘ └──────────┘ └──────────┘ └──────────┘    │ │
│  └──────────────────────────────────────────────────────────┘ │
│  ┌─ Quick Actions ───────────┐ ┌─ Custom Crawl ────────────┐ │
│  │ [Check Environment]       │ │ Mode: [Single Table ▼]   │ │
│  │ [Daily Update]            │ │ Table: [下拉选择表名 ▼]   │ │  ← text input 改 select
│  │ [Backfill (7 days)]       │ │ Season: [2025     ]       │ │
│  │ [List Available Tables]   │ │ Days: [7         ]        │ │
│  │                           │ │ [Start Crawl]             │ │
│  └───────────────────────────┘ └───────────────────────────┘ │
│  ┌─ Crawler Logs ───────────────────────────────────────────┐ │
│  │ [log output...]                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**数据概况区块设计要点：**
- 使用网格布局（flex-wrap 卡片或 grid），每个表展示为一张小卡片
- 卡片内容：表名（mono 字体加粗）、行数（数字带千分位）、简要说明（一行文字截断）、最后更新时间（相对时间格式，如"3 小时前"）
- 点击卡片表名可自动填充爬取表下拉菜单
- 卡片颜色根据数据状态变化：正常（绿色边框）、过期（黄色）、无数据（灰色）

**爬取表下拉菜单设计要点：**
- `<select>` 元素，选项 text = 表名，value = 表名
- 可按字母排序，方便定位
- 支持根据 mode 联动（某些表只在特定模式下可用）

#### 球队 Radar 图（Overlay 版改动后）

```
┌──────────────────────────────────────────────────────────────┐
│                      雷达图标题区域                            │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                       ┌─────┐                          │  │
│  │                    ╱   PTS   ╲                         │  │
│  │                  ╱  ┌─────┐   ╲                        │  │
│  │                ╱    │ ●  │     ╲  AST                  │  │
│  │              ╱   ┌──┴─────┴──┐   ╲                     │  │
│  │            REB ◄── ● ──── ● ─────► STL                 │  │
│  │              ╲   └──┬─────┬──┘   ╱                     │  │
│  │                ╲    │ ●  │     ╱                       │  │
│  │                  ╲  └─────┘   ╱                        │  │
│  │                    ╲         ╱                         │  │
│  │                     └───┬───┘                          │  │
│  │                         BLK                            │  │
│  │  ───── LAL (实线, 绿色)                                 │  │
│  │  - - - 联盟平均 (虚線, 灰色)                              │  │
│  └────────────────────────────────────────────────────────┘  │
│  [图例: ● LAL  ● 联盟平均]                                    │
└──────────────────────────────────────────────────────────────┘
```

**设计要点：**
- 双系列：球队系列使用实线 + 高亮填充色，联盟平均系列使用虚线 + 半透明填充色
- 图例固定在底部，显示球队名（如 "LAL"）和 "联盟平均"
- Tooltip 触发时显示两行数据对比
- 所有雷达指标与 `renderTeamRadarChart` 保持一致

#### Workspace 页面布局（改动后）

```
┌──────────────────────────────────────────────────────────────┐
│  [page-header] Workspace: "2024-25 球队进攻分析"    [保存]   │
├──────────────────────────────────────────────────────────────┤
│  ┌─ 已添加的公式 ──────────────────────────────────────────┐ │
│  │ ✔ EFF (效率值)       [移除]                              │ │
│  │ ✔ TS% (真实命中率)    [移除]                              │ │
│  │ ✔ PIE (球员影响力)    [移除]                              │ │
│  └──────────────────────────────────────────────────────────┘ │
│  ┌─ 可用预设公式 (一键添加) ────────────────────────────────┐ │ ← 新增区块
│  │ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐        │ │
│  │ │ EFF         │ │ TS%         │ │ PER (简化)   │        │ │
│  │ │ 效率值计算   │ │ 真实命中率   │ │ 球员效率值   │        │ │
│  │ │ [+ 添加]    │ │ [+ 添加]    │ │ [+ 添加]    │        │ │
│  │ └─────────────┘ └─────────────┘ └─────────────┘        │ │
│  │ ┌─────────────┐ ┌─────────────┐                        │ │
│  │ │ PIE         │ │ ORTG/DRTG   │                        │ │
│  │ │ 球员影响力   │ │ 攻防效率     │                        │ │
│  │ │ [+ 添加]    │ │ [+ 添加]    │                        │ │
│  │ └─────────────┘ └─────────────┘                        │ │
│  └──────────────────────────────────────────────────────────┘ │
│  ┌─ 数据集 ─────────────────────────────────────────────────┐ │
│  │ [添加数据集...]                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**设计要点：**
- "可用预设公式"区块以网格卡片展示，每张卡片包含公式名称、简短描述、添加按钮
- 已添加的公式在公式列表中显示 ✔ 标记，预设区域对应的卡片按钮变为灰色或隐藏
- 点击 [+ 添加] 直接调用 `api('/api/workspaces/{id}/formulas', { method: 'POST', body: { formula_id } })`
- 预设公式来源：后端 `GET /formulas/presets` 端点返回，或 formulas 表中 `is_preset = true` 的记录

### Open Questions

#### 需求一：爬虫页面改版

1. **数据概况的数据源**：表概览数据是从 information_schema 统计（实时但计算开销较大），还是用爬虫元数据表（如 `crawl_meta` 或 `table_registry`）？建议使用后者，因为 information_schema 无法提供"简要说明"字段。
2. **表说明的来源**：数据库表的中文说明/描述从哪里来？是否需要新增元数据配置表或配置文件？
3. **可爬取表列表的判定逻辑**：`GET /crawler/tables` 返回的表是固定白名单，还是自动扫描所有表？建议后端维护可爬取白名单，避免暴露敏感/系统表。
4. **数据概况刷新时机**：每次打开爬虫页面时加载，还是加一个手动刷新按钮？

#### 需求二：球队 Radar 图恢复联盟平均对比

1. **联盟平均数据是否需要多赛季对比**：当前只按单赛季加载，后续是否支持跨赛季趋势？
2. **雷达图指标的固定性**：`GET /teams/{abbr}/radar` 返回的指标字段是否与 `GET /teams/league-avg/radar` 完全一致？需要确保两端点返回的 indicator 的 field key 一致，以正确映射。
3. **错误处理**：如果联盟平均数据端点返回空，是否仍然显示单系列雷达图（降级策略）？

#### 需求三：Workspace 增加常用公式预设

1. **预设公式的存储方式**：建议采用 seed 数据方式（`is_preset = true` 字段），和用户自定义公式共用 formulas 表，方便统一管理。是否需要新增 `formula_presets` 独立表？
2. **预设公式是否允许编辑**：用户点击添加后，是直接把预设公式完整复制到 workspace，还是预设公式为只读引用？建议做"复制到自定义"——用户看到预设公式描述，一键添加到 workspace 时自动复制到用户的 formula 记录中，之后可自由修改。
3. **预设公式的多语言**：公式名称和描述是否需要 i18n 支持？
4. **ORTG/DRTG 是两个独立公式还是一个组合公式**：建议作为两个独立预设公式，也可以作为一个组合公式同时添加，需与业务确认。
