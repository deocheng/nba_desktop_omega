# 补爬计划：player_gamelog 完整性 + 身份缺口（2026-08-07）

> 范围：仅调研/规划。未启动任何爬虫、未改动代码/数据。所有结论来自 `verify_gaps.py --all`（权威门禁）与只读 SQL 校验。
> 原则：由近及远，单赛季为一个独立目标。

---

## 一、所有缺口清单

### 1A. 完整性缺口（常规赛 RS 比赛覆盖，来源 `verify_gaps --all`）

权威口径：`player_gamelog` 去重 `gameid` vs `dim_games(season_type='Regular Season')`，用 `EXISTS` 精确算缺失。

| 赛季 | RS 覆盖 | 缺失场 | 备注 |
|---|---|---|---|
| 2004 | 1182/1192 | 10 | 小缺口 |
| 2005 | 1215/1230 | 15 | |
| 2006 | 1214/1230 | 16 | |
| 2007 | 1215/1230 | 15 | |
| 2008 | 1215/1230 | 15 | |
| 2009 | 1223/1230 | 7 | |
| 2010 | 1222/1230 | 8 | |
| 2011 | 1222/1230 | 8 | |
| 2012 | 983/990 | 7 | |
| 2013 | 1215/1229 | 14 | |
| **2014** | **0/1230** | **1230** | **整季空** |
| **2015** | **0/1230** | **1230** | **整季空** |
| **2016** | **0/1230** | **1230** | **整季空** |
| **2017** | **0/1230** | **1230** | **整季空** |
| **2018** | **0/1230** | **1230** | **整季空** |
| **2019** | **0/1230** | **1230** | **整季空** |
| **2020** | **0/1059** | **1059** | **整季空** |
| **2021** | **0/1080** | **1080** | **整季空** |
| **2022** | **0/1230** | **1230** | **整季空** |
| 2023 | 1056/1230 | 174 | |
| 2024 | 1135/1230 | 95 | |
| 2025 | 1230/1230 | 0 | ✅ 完整 (rc=0) |
| 2026 | 1230/1230 | 0 | ✅ 完整 (rc=0) |

**合计缺失 RS 场 ≈ 11,133 场**，主体为 2014–2022 共 9 个空季（约 10,749 场），另加 2023/2024 部分缺口（269 场）与 2004–2013 小缺口（115 场）。

> 注：`verify_gaps` 另报「逐人完整性」缺场（如 2024 有 487 名球员缺 2,847 人次）——那是按 `player_per_game.g` 对比的球员-场次缺额，最终由同一套重爬补齐；上表「缺失场」是更直观的比赛级缺口，两者均由重爬覆盖。

### 1B. 身份缺口（`br_player_id` 为 NULL 但有 `player_name`）

- 数量：**4,308 行**，分布在 **2,136 场不同比赛**（赛季跨度 1982–2025）。
- 当前 `player_id` 列已统一为 BR slug（前序工作），故这 4,308 行即「有名字、无 BR 身份」的残留。
- 构成（修正后的正确分类，前次 staging 有 LEFT JOIN 计数 bug，已纠正）：
  - **4,022 行**：名字在 `dim_players` 中**查无此人**（多为 G-League/10 天/国际/边缘球员）。
  - **242 行**：同名多 slug 且赛季窗口内仍撞车（如 George Johnson 1983 → johnsge01/02/03）。
  - **44 行**：同名多 slug 但均不在窗口内。

---

## 二、消歧 / 线下重补 方法验证（结论）

| 方法 | 是否可行 | 证据 |
|---|---|---|
| **① 离线消歧（表内/跨表）** | ❌ 不可行（0/4,308） | 用 `norm_name()` + `good_name_map`(1.1M 干净行) + `dim_players`(year_from/year_to 窗口) 三档规则（同场精确 / 队+季唯一 / 名字全局唯一 / 窗口唯一）分类：RESOLVABLE=0、ABSENT=4,022、AMB_COLLISION=242、AMB_MULTI_NOWIN=44。名字不在 `dim_players` 或本身就是同名，无法靠现有数据唯一确定 slug。 |
| **② 线下重补（重解析缓存的 boxscore HTML）** | ❌ 不可行 | `raw_archive/br/{year}/{game_id}.html` 仅含 **6 个稀疏年目录**（1997–2000、2024、2026），对 2,136 场受影响比赛 **命中 0 场**。缓存未覆盖，无法离线重解析。 |
| **③ 逐球员页重爬补齐（现有 `recrawl_*`）** | ⚠️ 不覆盖这批 | 重爬走 `verify_gaps --missing-ids` → 只抓 `player_per_game` 中缺场球员；而 4,308 行均**不在 `player_per_game`**，故重爬不会触达，slug 仍空。 |

**结论**：4,308 行身份缺口**当前无任何离线/重爬手段可自动恢复**。其 slug 仅存在于原始 boxscore 页面（每条球员名旁有 `/players/X/{slug}.html` 锚点），而该页面既未缓存、现有爬虫也不解析 boxscore。
**处置建议（待你拍板）**：按你「过后辨别回填」的模型，**这 4,308 行暂留 `named-null`**——它们仍可按 `(player_name, team, season, gameid)` 分组使用，不影响完整性门禁。slug 的彻底恢复需另立专项：① 新建 boxscore 解析器提取 slug 锚点；② 对受影响比赛重抓 boxscore（属一次独立爬取，不在本计划内）。

---

## 三、补爬计划（由近及远，单季独立目标）

### 3.1 现有机制（已具备，无需改代码）
`external_crawler/crawler/recrawl_2004_2026_autofill.sh` 特性：
- 赛季顺序 `2026→2004`（近→远），**每季为循环内独立单元**。
- **智能补齐**：`verify_gaps.py $S --missing-ids` 取缺口球员 → 仅爬这些（`--only-br-ids`），空季则退化为整季全量。
- **权威门禁**：每季跑完即 `verify_gaps.py $S`，要求 `rc=0` **且** `minutes` 填充率 ≥ 99%（防 2026 曾出现的 65% 脏批误判完成）。
- **重试**：单季最多重试 5 次；达标写 `STATE` 跳过，未达标入 `FAILURES` 并继续下一季（不空转）。
- **单季即停**：写 `logs/stop_after_season.flag`=某季，该季跑完即停 → 天然支持「单赛季为单独目标、逐季验收」。

### 3.2 前置条件（执行前需确认）
1. **Chrome CDP @9223 活体**（`launch_chrome_cdp.sh` 启动，`cf_clearance` 在磁盘 profile）。脚本启动即探活，不可达则直接停。
2. **预置 STATE 跳过已完成季**：将 `2025`、`2026` 写入 `logs/recrawl_done_2004_2026.txt`，避免对已完成季空跑全量（节省数百球员页抓取）。*（写标记文件属低风险配置，可随执行一并做）*
3. 不在本计划内处理 4,308 身份缺口（见第二节）。

### 3.3 执行顺序（近→远，每季独立目标）
```
2024 → 2023 → 2022 → 2021 → 2020 → 2019 → 2018 → 2017
→ 2016 → 2015 → 2014 → 2013 → 2012 → 2011 → 2010
→ 2009 → 2008 → 2007 → 2006 → 2005 → 2004
```
- **2025 / 2026**：已 rc=0，仅做终检，不重抓。
- **2024 / 2023**：小-中缺口（95 / 174 场），优先近季快速收口。
- **2022 → 2014**：9 个**空季**（大头，约 10,749 场），每季需抓数百名球员页。
- **2013 → 2004**：小缺口为主（7–16 场/季），收尾。

### 3.4 每季标准步骤
1. `crawl_br_gamelog.py --season $S [--only-br-ids /tmp/gap_$S.txt]`（脚本自动驱动）。
2. 门禁：`verify_gaps.py $S` → `rc=0` 且 `minutes≥99%`。
3. 不达标 → 幂等重试（≤5，仅补缺口球员）。
4. 达标 → 追加 `$S` 到 `STATE`；如需逐季验收则写 `stop_after_season.flag=$S` 跑完即停。
5. 进入下一季。

### 3.5 可选：逐季手动驱动（更贴合「单季独立目标」）
不跑 autofill 长链，而是**逐季手动触发 + 即时验收**：
```bash
cd external_crawler/crawler
# 单季：智能补齐缺口
python3 crawl_br_gamelog.py --season 2024 --cache-dir gamelog_cache
# 门禁验收
python3 verify_gaps.py 2024      # 必须 rc=0 且 minutes≥99%
```
跑通一季、你确认后，再推进下一季。控制粒度最细，但需人工逐季推进。

---

## 四、风险与注意
- **空季是大头**：2014–2022 共 ~10,749 场，每季数百球员页，耗时最长、最易遇 CF 熔断/限速。看门狗熔断→长睡→续跑机制已具备；失败季入 `FAILURES`，终检 `verify_gaps --all` 列出真实缺口。
- **重爬后可能并存 slug 行 + 旧 null 行**：重爬为 `player_per_game` 内球员写入带 slug 的新行，旧 4,308 行（非 `player_per_game`）因键不同不会被覆盖，也不产生重复；但历史上曾出现同场重复行（前序已 DELETE 128 行）。每季跑完建议跑一遍「同 `(gameid, br_player_id)` 重复行」清理（参考 `ux_player_gamelog_game_player` 唯一约束），确保零冗余。
- **4,308 身份缺口不在本计划范围**：重爬不解决，按其「过后辨别回填」模型暂留 `named-null`。
- **分钟数门禁**：`build_insert` 已修 M:SS 解析，重爬须确保 `minutes` 填充率达标，否则门禁不通过、触发重试。

---

## 五、验收标准
- 终检 `verify_gaps --all` → 全季 `rc=0`、RS 覆盖 100%（允许极少量全 DNP 场 <0.5% 偏差）。
- 每季在 `STATE` 有记录；`FAILURES` 为空（或仅含已人工确认可豁免的场）。
- `player_gamelog` 全表 `player_id` = `br_player_id`（BR slug 格式，零失配）——已在前序工作达成，重爬只增不改格式。
